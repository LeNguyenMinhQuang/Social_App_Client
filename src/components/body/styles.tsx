import styled from "styled-components";

export const Wrapper = styled.div`
  background-color: var(--bg-1);

  .container {
    margin-block: 88px;
    max-width: 1600px;
    padding-inline: 24px;
  }
`;
